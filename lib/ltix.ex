defmodule Ltix do
  @moduledoc """
  An Elixir library for building LTI 1.3 tool applications.

  ## Main Modules

  - `Ltix.Registration` — Platform registration data (issuer, client_id, endpoints)
  - `Ltix.Deployment` — A specific tool installation on a platform
  - `Ltix.StorageAdapter` — Behaviour your app implements for storage
  - `Ltix.LaunchClaims` — Parses JWT launch payloads into structured data
  """
end
